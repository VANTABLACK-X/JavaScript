function reverseNumber(num) {
    // Convert the number to a string, reverse it, and convert back
    let str=" ";
    str = num.toString();
    return str;
  }
  console.log(reverseNumber(9876));  // Output: 6789
  